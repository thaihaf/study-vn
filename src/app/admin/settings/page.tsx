import { requireUser } from '@/modules/auth/session'; export default async function Page(){const user=await requireUser();return <><h1 style={{fontSize:'2.5rem'}}>Cài đặt hệ thống</h1><div className="card"><h2>AI</h2><p>Provider: {process.env.AI_PROVIDER??'openai'} · Model: {process.env.OPENAI_MODEL??'chưa cấu hình'} · API key: {process.env.OPENAI_API_KEY?'đã cấu hình':'chưa cấu hình'}</p><p className="muted">Chỉ biến môi trường phía server được sử dụng. Khóa API không được gửi tới trình duyệt.</p></div><div className="card"><h2>Phiên hiện tại</h2><p>{user.role} · xuất bản: {user.canPublish?'có':'theo chính sách vai trò'}</p></div></>}
