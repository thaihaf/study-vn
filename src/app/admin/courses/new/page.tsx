import { createCourse } from '@/app/actions'; import { requirePermission } from '@/modules/auth/session';
export default async function NewCourse(){await requirePermission('course:edit');return <><h1 style={{fontSize:'2.5rem'}}>Khóa học mới</h1><form className="card grid" action={createCourse}><label className="label">Tên khóa học<input className="input" name="title" required minLength={3}/></label><label className="label">Mô tả ngắn<textarea className="input" name="description" required minLength={10}/></label><div className="grid" style={{gridTemplateColumns:'1fr 1fr'}}><label className="label">Danh mục<input className="input" name="category" required/></label><label className="label">Trình độ<select className="input" name="level"><option>Cơ bản</option><option>Trung cấp</option><option>Nâng cao</option></select></label></div><button className="btn">Tạo bản nháp</button></form></>}
