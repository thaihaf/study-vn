import { requireUser } from '@/modules/auth/session'; export default async function Page(){await requireUser();return <div className="container page"><h1>Phiên luyện phỏng vấn</h1><div className="card"><label className="label">Câu trả lời của bạn<textarea className="input" rows={12}/></label><p className="muted">Rubric được lưu cùng lần nộp để việc so sánh minh bạch.</p><button className="btn">Lưu bản nháp</button></div></div>}
