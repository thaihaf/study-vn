import Link from 'next/link'; import { login } from '@/app/actions';
export default function Login(){return <div className="container page" style={{maxWidth:480}}><form className="card grid" action={login}><h1 style={{fontSize:'2rem'}}>Chào bạn trở lại</h1><label className="label">Email<input className="input" name="email" type="email" required autoComplete="email"/></label><label className="label">Mật khẩu<input className="input" name="password" type="password" minLength={8} required autoComplete="current-password"/></label><button className="btn">Đăng nhập</button><Link className="muted" href="/forgot-password">Quên mật khẩu?</Link><Link href="/register">Chưa có tài khoản? Đăng ký</Link></form></div>}
